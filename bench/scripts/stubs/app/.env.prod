# Bench engine defaults for APP_ENV=prod. Loaded by Symfony Dotenv AFTER .env;
# real environment variables (compose / Dockerfile ENV) always take precedence.
APP_DEBUG=0
APP_SECRET=bench-only-not-a-secret
DB_HOST=bench-postgres
DB_PORT=5432
DB_NAME=waffle
DB_USER=waffle
DB_PASSWORD=waffle
